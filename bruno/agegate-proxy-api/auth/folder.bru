meta {
  name: Auth
  seq: 1
}

docs {
  Rotas de autenticação para usuário final e admin.

  Use `User Login` antes das rotas em `user/*`.
  Use `Admin Login` antes das rotas em `admin/*`.

  O Bruno mantém o cookie de sessão entre requests, então não é necessário copiar token manualmente.
}
