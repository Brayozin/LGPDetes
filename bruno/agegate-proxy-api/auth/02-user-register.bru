meta {
  name: User Register
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/register
  body: json
  auth: none
}

body:json {
  {
    "fullName": "{{registerFullName}}",
    "email": "{{registerEmail}}",
    "password": "{{registerPassword}}",
    "age": {{registerAge}}
  }
}
