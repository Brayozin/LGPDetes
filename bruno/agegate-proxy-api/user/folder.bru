meta {
  name: User
  seq: 3
}

docs {
  Rotas usadas pelo usuário autenticado dentro do LGPDetes Proxy.

  Antes de usar esta pasta:
  1. rode `auth/User Login`
  2. defina `platformId`, `providerId` e, quando aplicável, `clientSessionId`
}
